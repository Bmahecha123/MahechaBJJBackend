server:
  port: ${serverPort:8080}

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/user

---

spring:
  profiles: cloud
  data:
    mongodb:
      uri: ${vcap.services.mongo-mahechabjj.credentials.uri}